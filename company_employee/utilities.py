class Utilities:

    # This method will check if both strings are the same
    def check_values(self, user_value, second_value):
        if user_value.lower() == second_value.lower():
            return True
        else:
            return False